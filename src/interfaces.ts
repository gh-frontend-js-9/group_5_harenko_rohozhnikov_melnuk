export interface IState {
    popular: object,
    essential: object,
    freelance: object
}

export interface IProps {
    fetchData: any,
    fetchRequest: any
}

/////// SERVICE AREA
/*
interface Irops {
    title: string,
    description: string,
    category: string[],
    tags: string
}

interface Iops {
    docs: any,
    total: number,
    limit: number,
    page: number,
    pages: number
} */
///////